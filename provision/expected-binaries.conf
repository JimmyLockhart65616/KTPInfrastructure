# KTP Fleet — Expected binary md5s (source of truth)
#
# Used by scripts/audit-fleet-drift.py to detect drift between the live fleet
# state and what the repo says each host SHOULD have. Values are the first
# 16 chars of md5sum output (matches the fleet-drift-snapshot.sh format).
#
# Update after each successful fleet deployment. Canary instances will flag
# as drift until the fleet-wide promotion completes — that's expected and
# informative.
#
# Format: one `relative/path = md5prefix16` per line. Paths are relative to
# each host's ~/dod-27015/serverfiles/ (the audit checks port 27015). `#`
# starts a comment; [section] headers scope expected values by host group.
#
# [default] applies to all hosts unless a [baremetal] / [vps] section
# overrides. Group overrides are useful if the VPS runs a different subset
# of modules — currently all KTP binaries should be identical fleet-wide.

[default]

# ============================================================================
# Current production baseline (updated 2026-04-20 for 917 promotion)
# ============================================================================
#
# KTP-ReHLDS 3.22.0.917 promotion: .new files deployed to 23 fleet instances
# on 2026-04-20 afternoon (all hosts except Denver 27019 which was the canary,
# and Chicago 27019 which is .ktp-disabled). The nightly ktp-scheduled-restart.sh
# at 03:00 ET on 2026-04-21 will swap engine_i486.so.new + hlds_linux.new into
# place. Until that swap completes, running this audit will show 23 hosts as
# drifted BELOW expected (still on de5bc08f/0162dcb2) — that's the expected
# pre-swap state, not a real drift.
#
# Canary (Denver 27019) + NY (all 5, for hlds_linux) already match the new
# baseline; they'll audit clean immediately.

# KTP-ReHLDS 3.22.0.917 (promoted 2026-04-20, swap at 2026-04-21 03:00 ET)
engine_i486.so = 9f263a3ff2f97f26
hlds_linux = 0b06fef914371de3
libsteam_api.so = cf904f9dd3ef1118

# KTPAMXX 2.7.10 (pre-2.7.11 production baseline)
dod/addons/ktpamx/dlls/ktpamx_i386.so = 8c3185172177e89f

# DODX 2.7.10
dod/addons/ktpamx/modules/dodx_ktp_i386.so = 92d77121e8eac25f

# KTP-ReAPI
dod/addons/ktpamx/modules/reapi_ktp_i386.so = d1da457afc9e23e6

# KTPAmxxCurl 1.3.8-ktp (promoted fleet-wide 2026-04-19 overnight restart)
dod/addons/ktpamx/modules/amxxcurl_ktp_i386.so = f2cd4f5d067dc487
